export default class Canvas{
    constructor(element, context, width, height){
        this.element = element;
        this.ctx = context;
        this.width = width;
        this.height = height;
    }
}